// import { Action, createReducer, on } from '@ngrx/store';
// import { Cities } from './location.actions';

// export const key = 'location';

// export interface LocationState {
//   city: Cities;
// }

// export const initialState: LocationState = { city: undefined };

// const locationReducer = createReducer(initialState,
//   on(setCity, (state, { city }) => ({
//       ...state,
//       city
//     }))
// );

// export function reducer(state: LocationState, action: Action) {
//   return locationReducer(state, action);
// }
